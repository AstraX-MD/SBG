services:
  - type: web
    name: sbg-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_ID
        sync: false
      - key: GROQ_API_KEY
        sync: false